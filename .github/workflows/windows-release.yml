name: Desktop Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  export-windows:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Stamp build version
        shell: pwsh
        env:
          BUILD_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
        run: |
          ((Get-Content project.godot) -replace '^config/version=.*$', "config/version=`"$env:BUILD_VERSION`"") | Set-Content project.godot

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.1
          use-dotnet: false
          include-templates: true

      - name: Export Windows build
        run: |
          mkdir -p .exports/windows .exports/server dist
          godot --headless --path . --export-release "Windows Desktop" .exports/windows/ClaudeOtherGods.exe
          godot --headless --path . --export-release "Windows Dedicated Server" .exports/server/ClaudeOtherGodsServer.exe

      - name: Package release zips
        run: |
          cd .exports/windows
          zip -9 -r ../../dist/ClaudeOtherGods-windows.zip *
          cd ../server
          zip -9 -r ../../dist/ClaudeOtherGodsServer-windows.zip *

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClaudeOtherGods-windows
          path: |
            dist/ClaudeOtherGods-windows.zip
            dist/ClaudeOtherGodsServer-windows.zip

      - name: Publish GitHub release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/ClaudeOtherGods-windows.zip
            dist/ClaudeOtherGodsServer-windows.zip

  export-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Stamp build version
        shell: pwsh
        env:
          BUILD_VERSION: ${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
        run: |
          ((Get-Content project.godot) -replace '^config/version=.*$', "config/version=`"$env:BUILD_VERSION`"") | Set-Content project.godot

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.1
          use-dotnet: false
          include-templates: true

      - name: Export macOS build
        run: |
          mkdir -p .exports/macos dist
          godot --headless --path . --export-release "macOS" .exports/macos/ClaudeOtherGods-macos.zip
          cp .exports/macos/ClaudeOtherGods-macos.zip dist/ClaudeOtherGods-macos.zip

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClaudeOtherGods-macos
          path: dist/ClaudeOtherGods-macos.zip

      - name: Publish GitHub release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: dist/ClaudeOtherGods-macos.zip
