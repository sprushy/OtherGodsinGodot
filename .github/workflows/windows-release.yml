name: Windows Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  export-windows:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Godot
        uses: chickensoft-games/setup-godot@v2
        with:
          version: 4.6.1
          use-dotnet: false
          include-templates: true

      - name: Export Windows build
        run: |
          mkdir -p .exports/windows .exports/server dist
          godot --headless --path . --export-release "Windows Desktop" .exports/windows/ClaudeOtherGods.exe
          godot --headless --path . --export-release "Windows Dedicated Server" .exports/server/ClaudeOtherGodsServer.exe

      - name: Package release zips
        run: |
          cp .exports/windows/ClaudeOtherGods.exe dist/ClaudeOtherGods.exe
          cp .exports/server/ClaudeOtherGodsServer.exe dist/ClaudeOtherGodsServer.exe
          cd dist
          zip -9 -r ClaudeOtherGods-windows.zip ClaudeOtherGods.exe
          zip -9 -r ClaudeOtherGodsServer-windows.zip ClaudeOtherGodsServer.exe

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: ClaudeOtherGods-windows
          path: |
            .exports/windows/ClaudeOtherGods.exe
            .exports/server/ClaudeOtherGodsServer.exe
            dist/ClaudeOtherGods-windows.zip
            dist/ClaudeOtherGodsServer-windows.zip

      - name: Publish GitHub release assets
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            .exports/windows/ClaudeOtherGods.exe
            .exports/server/ClaudeOtherGodsServer.exe
            dist/ClaudeOtherGods-windows.zip
            dist/ClaudeOtherGodsServer-windows.zip
